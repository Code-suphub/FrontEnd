server:
  port: 8181

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/forum?useUnicode=true&useJDBCCompliantTimezoneShift=true&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    username: 23339097@qq.com # 发送人邮箱
    password: gsagrtgbwgicbhci # 邮箱授权码(上面获取到的)
    host: smtp.qq.com # 邮件服务器,163邮箱就是smtp.163.com
    protocol: smtp # 协议，可以不配，默认就是这个
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  redis:
    host: localhost
    port: 6379
#    password: root
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 100ms
mybatis-plus:
  #  mybatis-plus??sql??
  # configuration:
  # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:com/ttice/mapper/xml/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  #  ??ssh https
#  ssl:
#    key-store: classpath:Myssh.pfx
#    key-store-password: ko674hz8x35
#    key-store-type: PKCS12
